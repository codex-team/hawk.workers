import LimiterWorker from "../src";

describe('Limiter worker', () => {
  const worker = new LimiterWorker();

  test('Should save current events count for workspace from last charge date', () => {

  });
  test.skip('Should ban projects in workspace that exceed the limit of events on tariff plan');
});
